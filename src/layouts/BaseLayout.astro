---
const { title = "Perspectiva Cristã", description = "Reflexões, fé e propósito." } = Astro.props;
---
<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        {/* BASE_URL mantém navegação correta em subpasta do GitHub Pages */}
        <a class="logo" href={import.meta.env.BASE_URL}>Perspectiva Cristã</a>
        <nav class="site-nav">
          {/* BASE_URL evita redirecionar para a raiz do domínio */}
          <a href={import.meta.env.BASE_URL}>Início</a>
        </nav>
      </div>
    </header>
    <main class="container">
      <slot />
    </main>
    <footer class="site-footer">
      <div class="container">
        <p>Projeto editorial pessoal. Conteúdo autoral e em constante construção.</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: light;
    font-family: "Inter", "Segoe UI", system-ui, -apple-system, sans-serif;
    background-color: #f7f5f2;
    color: #1f1f1f;
    line-height: 1.7;
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  img {
    max-width: 100%;
    display: block;
  }

  .container {
    width: min(960px, 92vw);
    margin: 0 auto;
  }

  .site-header {
    background: #ffffff;
    border-bottom: 1px solid #e6e0d9;
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .site-header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.25rem 0;
  }

  .logo {
    font-weight: 700;
    font-size: 1.25rem;
    letter-spacing: 0.02em;
  }

  .site-nav a {
    font-size: 0.95rem;
    color: #4a3f35;
  }

  main {
    padding: 2.5rem 0 4rem;
  }

  .site-footer {
    border-top: 1px solid #e6e0d9;
    padding: 2rem 0;
    font-size: 0.9rem;
    color: #6d6156;
    background: #ffffff;
  }
</style>
